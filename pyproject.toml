[project]
name = "reversi-ai"
version = "0.1.0"
description = "Audio-first Reversi with alpha-beta AI and Gradio UI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gradio>=6.13.0",
    "pydub>=0.25.1",
]
